# Agent Meeting · 交接清单

> 整理：阿编 🎬 ｜ 用途：新会话 / 外部接手（azhu 验收）的唯一依据
> 定位：把 **WorkBuddy 以 Agent 身份接入 Agent Hub 群聊会议系统** 的开源方案（前端网页 + 后端服务 + WorkBuddy 技能 三件套同仓）。

---

## 0. 一句话接手须知
- **设计铁律**：技能（`skill/loop.py`）只做「手」（register / pull / reply / 已读 / 持久化），所有**思考由接入的 AI（WorkBuddy）在对话里完成**；脚本不内置自动大脑、无后台自循环。
- **代码仓库**：GitHub `shijieweb/agent-meeting`，统一推 `main`（前端/后端/技能同仓提交）。
- **密钥**：一律不进代码；运行时从环境变量 / 部署机本地读取。

---

## 1. 架构与目录
```
agent-meeting/
├── skill/          # WorkBuddy 技能（安装时只部署这一层）
│   ├── SKILL.md    # 技能主文档（4 方法详细执行流程）
│   ├── loop.py     # 连接器（纯标准库，4 方法 CLI: init/pull/reply/end）
│   └── install.sh / install.ps1   # 一键安装到 ~/.workbuddy/skills/agent-meeting/
├── server/         # Agent Hub 后端 + 前端
│   ├── app/main.py # 入口（uvicorn app.main:app）
│   ├── app/routers/    # /agents /messages 路由
│   ├── app/services/   # 存储 / 消息去重 / Agent 注册
│   ├── app/static/     # 网页前端（index.html / app.js / styles.css）
│   └── legacy/         # 早期 Flask 5000 原型（协议不兼容 8000，勿直跑）
├── proposals/      # 演进方案（A1 合并计划 / A2 评审流 / A3 文档改进）
├── README.md / 需求文档.md / e2e-ui-test-report.md / qa-read-receipt-regression.md
```

---

## 2. 端口与访问路径
| 入口 | 地址 | 说明 |
|---|---|---|
| 后端直连 | `http://localhost:8000` | FastAPI 服务（消息持久化 / 未读去重 / Agent 注册与会话态） |
| 走门户 | `http://<机器>:8787/meeting/` | 经短剧主项目 `agnes_proxy.py` 反代到 8000（老板只访 8787） |
| 外网 | `http://agnes.owen1.de5.net/meeting/` | 公网反向代理入口 |

---

## 3. 启动与净重启（★ 铁律）
**日常启动（默认 --reload，老板拍板）**
```bash
cd agent-meeting/server
pip install -r requirements.txt        # fastapi / uvicorn[standard] / pydantic
# 本机 Python 3.14 路径示例：
C:/Users/67972/AppData/Local/Programs/Python/Python314/python.exe -m uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload --log-level warning
```

**净重启（仅当「curl 复验四项仍答旧码」才降级用，去 --reload 单进程）**
1. `Get-CimInstance Win32_Process` 按 `CommandLine` 含 `uvicorn`+`8000` 定位**真活 worker**（含 `multiprocessing-fork` 子进程，非仅父 reloader）；
2. `taskkill /F /PID <worker> /T` 杀整树（Stop-Process 只杀父会留孤儿答旧码）；
3. `find server/app -name "__pycache__" -type d -exec rm -rf {} +` 清全部 .pyc（git checkout 可能让陈旧字节码先生效）；
4. 不加 `--reload` 起单进程验证，复验新码生效：**四项全过 = 新码**
   - `POST /api/messages/send` `sender_type=agent` → **422**
   - `GET /api/agents/NotReg/session` → **400**
   - 5000 字 `reply` → **400**
   - `POST /api/messages/cleanup {}` → **400**

---

## 4. 会议模式（A' 本人大脑）
- **唤醒**：对话里说「**开会**」/「**上线开会**」→ 技能 `init` 上线 → 循环 `pull`（拉老板网页消息）→ 本人思考 → `reply` 发回 → 再 `pull` → `end`。
- **停止**：老板说「**结束会议**」才停；`loop.py` 是纯命令式、无内置循环，仅有本人调用时跑一次。
- 空也持续轮询（每 3 秒一次，最多 1000 次 ≈ 50min），老板网页发一句即刻接住。

---

## 5. 数据目录（相互独立，勿混淆）
| 目录 | 归属 | 说明 |
|---|---|---|
| `server/data/` | 后端 | 真实聊天数据（由 `config.py` 相对 `BASE_DIR/data` 决定）；**已被 .gitignore 排除，勿提交** |
| `~/.agent_hub/data` | 技能缓存 | `agent_name.txt` / `agent_read_*.json`（`AGENT_HUB_DATA_DIR` 可改） |

---

## 6. 安全硬化现状（已交付）
- `53f98e6` 文档系统安全修复：`DocCreate/DocEdit` 均 `extra="forbid"` 拦截 JSON body 多余字段。
- `0fe029b` 修复 AC-17.1：`doc_upload` 加 `request.form()` 校验 `sender_type/owner/owner_type` 等 forbidden 字段，命中即 400。
- 隔离测试用 `DATA_DIR` 指向独立目录（如 8011），**严禁直跑生产 `server/data`**（曾误删生产 ~237 条，已恢复 212）。
- 内部验收：53 子项全绿 + 三轮交叉测试全 PASS（功能 39 + 安全 14），`run_security_tests.py` / `test_security_quick.py` 已入库。

---

## 7. 已知待办（接手后必做）
| 项 | 内容 | 阻塞/依赖 | 状态 |
|---|---|---|---|
| **R1 令牌闸** | 8787 公网可达且无鉴权，文档写接口须加令牌校验（design.md F-1） | 待老板拍板节奏 | ⏸ 待拍板 |
| **R8 Agent 四态** | 在线/思考中/离线/等待 四态可见（基于 T-agent-presence-mgmt 三态扩展） | 待拍板 | ⏸ 待拍板 |
| **R3 消息搜索** | 消息搜索 + 按天归档，降低长会话查找成本 | 待拍板 | ⏸ 待拍板 |
| **azhu 外部验收** | 独立安全验收 AC-17/18/19/22（内部已全绿，需外部独立确认） | azhu 在会议室触发 | ⏸ 待触发 |
| 路线图其余 | R2/R4/R5/R6/R7/R9/R10（增强类） | 见 `dev-work/roadmap-agent-meeting.md` 末尾「老板拍板区」 | ⏸ 未定 |

> 节奏推荐（主理人建议，已写入 roadmap 但未获老板签字）：先 R1+R8+R3 → 再 R4+R5 → 后其余。

---

## 8. 关键文件速查
- 项目总览：`README.md`；后端设计：`server/设计文档.md`；后端接入：`server/接入指令书.md`
- 技能主文档：`skill/SKILL.md`；测试报告：`e2e-ui-test-report.md` / `qa-read-receipt-regression.md`
- 路线图（外部）：`dev-work/roadmap-agent-meeting.md`
- 安全验证脚本：`server/run_security_tests.py` / `server/test_security_quick.py`
