# 会议系统 需求规格（用户故事式 · v2.0）

> **版本**：v2.0 ｜ **日期**：2026-08-17 ｜ **维护**：阿编（分析师）
> **写法**：从用户角度逐条写需求，每条 = 用户故事 + 操作路径 + UI 行为 + 后端能力 + 状态数据。产品经理拿到本文件可直接写设计文档；开发据此写实现。
> **代码现状**：monorepo `agent-meeting/`（server/ 后端+前端、skill/ 连接器），运行时 `会议系统/agent_hub/`。

---

## 〇、角色与使用场景

| 角色 | 是谁 | 用什么 | 要什么 |
|---|---|---|---|
| **老板** | 人类主持人 | 浏览器网页（http://localhost:8000） | 给 AI 派活、看 AI 回复、随时掌握 AI 在线/收工状态 |
| **Agent（AI）** | WorkBuddy / Trae / openclaw 等接入工具 | HTTP 协议 + 连接器脚本 | 收到老板消息 → 真思考 → 回复；上线/收工状态可被老板看到 |
| **系统（Agent Hub）** | FastAPI 服务 | — | 只做消息传输与状态管理，**不替 AI 思考** |

**核心使用流程（一句话）**：老板在网页发消息 → 对应 Agent 拉取到（显示未读→已读）→ Agent 用本人推理回复 → 老板在网页看到回复与已读回执。

---

## 一、需求分类总览

| 分类 | 范围 | 需求数 |
|---|---|---|
| **A 网页端功能需求**（老板视角） | 打开网页 / 看状态 / 选对象 / 发消息 / 看消息流 / 看已读 / 自动刷新 / 异常处理 | A-1 ~ A-8 |
| **B Agent 接入需求**（AI 视角） | 上线 / 拉消息 / 回复 / 收工 / 状态刷新（last_seen）/ 幂等 / 断线容错 | B-1 ~ B-7 |
| **C 服务端支撑能力** | 注册 / 会话态 / 消息路由 / 已读回执 / 历史 / 健康检查 / 校验 | C-1 ~ C-7 |
| **D 数据与可靠性** | 持久化 / 原子写并发 / 损坏自愈 / 隔离配置 | D-1 ~ D-4 |
| **E 运维与部署** | 启动 / 冒烟测试 / 净重启 | E-1 ~ E-3 |
| **F 非功能与边界** | 内网部署 / 无鉴权 / 数据合规 | F-1 ~ F-3 |

---

## 二、A 类 · 网页端功能需求（老板视角）

> 使用环境：浏览器打开 `http://localhost:8000`，手机/桌面均可（viewport 自适应）。

### A-1 打开会议网页
**用户故事**：作为老板，我想在浏览器打开会议系统网页，以便给 AI 发消息、看 AI 回复。
**操作路径**：浏览器访问 `http://localhost:8000`。
**UI 行为**：
- 页面加载后显示标题「小伙伴会议系统」、右上角阿编状态点、中间消息区、底部输入区（对象选择下拉 + 输入框 + 发送按钮）。
- 加载时自动拉取已注册 Agent 列表、历史消息、阿编在线状态。
**后端能力**：`GET /` 返回 index.html；`GET /static/*` 返回静态资源；前端自动调 `GET /api/agents`、`GET /api/messages/history`、`GET /api/agents/status`。
**状态与数据**：无写入；页面初始渲染依赖三接口返回。

### A-2 查看阿编（Agent）在线状态
**用户故事**：作为老板，我想一眼看到阿编现在是在线处理、待命、还是收工/掉线，以便判断"该不该等他"。
**操作路径**：无需操作，状态点常驻页面右上角，每 3 秒自动刷新。
**UI 行为**（状态点 5 态 + 1 提示条）：
| 状态 | 显示 | 实际机制（服务端 status 字段 + 前端判定） |
|---|---|---|
| 处理中 | 🟢 阿编·处理中 | 触发条件：① Agent 上线（init，置 working）② Agent pull 时**拉到消息**（got_data=true）。**核心：Agent 获取到消息后自动显示处理中，没有其它机制；开会期间（session=true）空轮 pull 仍按 record_pull 翻为待命（黄），不强制锁绿（2026-08-17 决策：接受待命）** |
| 待命中 | 🟡 阿编·待命中 | Agent pull 时**没拉到消息**（got_data=false，即在线等待）；或已注册但 last_seen 在存活窗口内且非 working |
| 已收工 | ⚪ 阿编·已收工 | Agent 调 end（session=false）→ 服务端置 status=offline（正常「结束会议」收工） |
| 离线 | ⚪ 阿编·离线 | 服务端无记录 / 无 last_seen；或 last_seen 超存活窗口（非会话 120s）且无会话 |
| 已掉线·需重唤 | 🔴 阿编·已掉线·需重唤 + 顶部红条提示 | 会话仍 active（session=true）但 last_seen 超存活窗口（600s）——大脑循环意外中断 → 显示「⚠️ 阿编已掉线……在对话里说『开会』重新唤醒」 |
**后端能力**：
- 服务端 status 由两类动作唯一决定：`set_session`（init→working / end→offline）与 `record_pull`（拉到→working / 未拉到→waiting）；last_seen 每次 pull 刷新。
- `GET /api/agents/status` 返回 {name, last_seen, status, session}；前端再按存活窗口（会话中 600s / 非会话 120s）做超时补判（掉线/离线）。
**状态与数据**：只读；last_seen 由 Agent 每次 pull 时刷新。

### A-3 选择发送对象
**用户故事**：作为老板，我想选择把消息发给「指定 Agent」还是「@所有人」，以便定向派活或群发通知。
**操作路径**：点击输入区左侧下拉框，从列表选择。
**UI 行为**：下拉框第一项固定为「@所有人」，其后列出所有已注册 Agent 名字（来自服务端实时列表）。
**后端能力**：`GET /api/agents` 返回注册 Agent 名列表；下拉选项由前端动态渲染。
**状态与数据**：选择值仅存于页面内存，用于发送时决定 target_type（all/single）。

### A-4 发送消息
**用户故事**：作为老板，我想在输入框输入消息并发送给选定的对象，以便给 AI 派活或提问。
**操作路径**：输入框输入文字 → 点「发送」按钮，或键盘 Enter。
**UI 行为**：
- 发送后输入框清空，消息立即出现在消息区（右侧、用户样式），下方显示未读状态「○ 未读」。
- 2 秒内自动刷新后，若目标 Agent 已拉取，未读变为「✓ 已读」。
**后端能力**：`POST /api/messages/send`（body：content / target_type / target_agent_name / client_msg_id 可选）；target 为 single 且 Agent 未注册 → 400 不保存；空内容 → 422；落库 messages.json；返回 message_id。
**状态与数据**：新增一条 sender_type=user 的消息；初始 read_by 为空（未读）；@all 广播时所有 Agent 各自记已读。

### A-5 查看消息流（对话记录）
**用户故事**：作为老板，我想看到完整的对话记录（自己发的 + AI 回的），以便跟踪协作进度。
**操作路径**：打开网页即显示；每 2 秒自动刷新。
**UI 行为**：
- 用户消息：右侧气泡，纯文本。
- Agent 消息：左侧气泡，顶部显示 Agent 名字，正文支持 Markdown 渲染（#/##/### 标题、列表、引用、行内代码、加粗）。
- 新消息到达自动滚动到底部。
**后端能力**：`GET /api/messages/history` 返回全部消息（含 read_by）；前端每 2 秒拉取渲染。
**状态与数据**：只读；消息含 sender_type / sender_agent_name / target_type / created_at / read_by。

### A-6 查看已读回执
**用户故事**：作为老板，我想知道 AI 到底看没看到我的消息，以便判断"是没看到还是没回"。
**操作路径**：无需操作，已读状态显示在每条用户消息下方。
**UI 行为**（已读 4 态）：
| 场景 | 显示 |
|---|---|
| 单发（single）且目标已读 | ✓ 已读 |
| 单发且未读 | ○ 未读 |
| 广播（all）部分已读 | N/N 已读 |
| 广播全部已读 | ✓✓ 全部已读 |
**后端能力**：已读数据来自消息 read_by 字段；Agent 调 `pull` 时服务端自动写入 read_by（拉取即已读）。
**状态与数据**：只读展示；写入动作由 Agent 的 pull 触发。

### A-7 自动刷新消息与状态
**用户故事**：作为老板，我不想手动刷新页面就能看到新消息和状态变化。
**操作路径**：无，页面自动。
**UI 行为**：历史消息每 2 秒轮询刷新；阿编状态每 3 秒刷新；刷新不打断页面交互。
**后端能力**：`GET /api/messages/history` + `GET /api/agents/status` 均为轻量只读接口，支持高频轮询。
**状态与数据**：只读。

### A-8 异常与弱网处理
**用户故事**：作为老板，当服务端暂时不可用或接口报错时，我不想页面崩溃、聊天记录丢失。
**操作路径**：无。
**UI 行为**：状态接口异常时静默跳过（不阻断聊天）；消息接口异常时页面保持上次渲染内容。
**后端能力**：接口 4xx/5xx 均返回明确错误体；服务恢复后轮询自动续上。
**状态与数据**：无写入；异常不影响已落库数据。

---

## 三、B 类 · Agent 接入需求（AI 视角）

> 接入方式：连接器 `skill/loop.py`（4 原子方法）或任意 HTTP 客户端。**AI 只负责思考，连接器只负责传输。**

### B-1 上线初始化（init）
**用户故事**：作为 Agent（AI），我想上线并让老板在网页看到我「处理中」，以便开始接收任务。
**操作路径**：老板在对话说「开会 / 上线开会」→ AI 调 `init --name <名字>`。
**UI/系统行为**：写本地名（agent_name.txt）→ 幂等注册 → 置会话 active → 网页状态点变「🟢 处理中」。
**后端能力**：`POST /api/agents/register`（幂等）+ `POST /api/agents/{name}/session?active=true`。
**状态与数据**：agents.json 记录 status=working、session=true。

### B-2 拉取未读消息（pull）
**用户故事**：作为 Agent，我想只拿到「老板发给我的、且我还没看过的」消息，以便不重复处理。
**操作路径**：AI 调 `pull`（轮询，默认每 3 秒一次、最多 1000 次；`--max 1` 单次探测）。
**UI/系统行为**：返回未读消息数组（拉到即返回）；空则继续等待；连接断不退出继续等；拉取同时刷新 last_seen。
**后端能力**：`GET /api/messages/pull?agent_name=`——只返回 target==我 或 target==all 且未读的；**拉取即标记已读**（写 read_by）；新 Agent 首拉用已读集合播种（不回灌历史）。
**状态与数据**：reads.json 更新；消息 read_by 追加；agents.json last_seen 更新。

### B-3 回复消息（reply）
**用户故事**：作为 Agent，我想把我思考后的回答发回给老板，并关联到老板问的那条消息。
**操作路径**：AI 对 pull 到的每条消息真思考 → 调 `reply --msg-id <id> --msg <内容>`（长文用 `--file`）。
**UI/系统行为**：回复以 Agent 气泡出现在网页消息区（左侧、Markdown 渲染）；若回复期间有新消息，返回体 new_messages 顺带带回。
**后端能力**：`POST /api/messages/reply`（agent_name / content / reply_to_message_id / client_msg_id 可选）；未注册 Agent → 400；空内容 → 422；client_msg_id 幂等去重。
**状态与数据**：新增 sender_type=agent 消息，挂在 reply_to_message_id 下。

### B-4 收工（end）
**用户故事**：作为 Agent，我想在老板说「结束会议」时下线，让老板看到我已收工。
**操作路径**：老板说「结束会议」→ AI **先 pull 一次确认无新指令**（铁律 I-10）→ 调 `end`。
**UI/系统行为**：网页状态点变「⚪ 阿编·已收工」。
**后端能力**：`POST /api/agents/{name}/session?active=false` → status=offline。
**状态与数据**：agents.json 记录 status=offline、session=false。

### B-5 在线状态刷新（last_seen）
**用户故事**：作为 Agent，我想让老板随时知道我是处理中/待命/已收工/掉线，以便老板判断协作节奏。
**操作路径**：无需主动操作——每次 pull 即刷新 last_seen。
**UI/系统行为**：网页状态点按 A-2 规则展示 5 态；会话中 last_seen 存活窗口放宽至 600s（大脑思考时间不计入掉线）。
**后端能力**：`GET /api/agents/status` 计算返回。
**状态与数据**：只读展示。

### B-6 幂等与去重
**用户故事**：作为 Agent，我想避免网络重试导致同一回复发两遍。
**操作路径**：reply 时 client_msg_id 用 `"c_"+原消息id`。
**UI/系统行为**：重复提交同 client_msg_id → 服务端返回 duplicate:true，不落第二条。
**后端能力**：message_store 按 client_msg_id 去重（send/reply 均支持）。
**状态与数据**：messages.json 不产生重复记录。

### B-7 断线与重试容错
**用户故事**：作为 Agent，当网络抖动或服务端短暂离线时，我不想整个会话崩溃。
**操作路径**：无。
**系统行为**：pull 对连接错误容错（继续轮询）；其余方法 5xx/连接失败按指数退避重试 3 次（1s/2s/4s），重试耗尽才报错。
**后端能力**：服务端无状态依赖，重启/恢复后轮询自动续上。
**状态与数据**：无。

---

## 四、C 类 · 服务端支撑能力

### C-1 Agent 注册
**需求**：Agent 接入第一步必须注册；同名重注册幂等（不重复建）。名字 1..200 字符，非空。
**能力**：`POST /api/agents/register` → 200 已注册/已存在（already_exists=true）。
**数据**：agents.json。

### C-2 会话态管理
**需求**：Agent 上线（active=true→working）与收工（active=false→offline）状态可切换，供网页展示。
**能力**：`POST /api/agents/{name}/session?active=`。
**数据**：agents.json。

### C-3 消息路由（single / all）
**需求**：用户消息可定向发给单个 Agent 或广播给全体；single 目标未注册 → 400 不保存；@all 每个 Agent 各自记已读。
**能力**：`POST /api/messages/send` + pull 按 target 过滤。
**数据**：messages.json。

### C-4 已读回执
**需求**：Agent 拉取即已读（服务端写 read_by），网页展示 4 态已读；未读去重完全在服务端（客户端零去重文件）。
**能力**：message_store.mark_read + storage 已读集合；并发拉取同一未读由全局锁保证只一人拿走。
**数据**：reads.json + 消息 read_by。

### C-5 历史查询
**需求**：网页端展示全部对话记录；调试/恢复用。
**能力**：`GET /api/messages/history` 返回全部消息（含 read_by）。
**数据**：messages.json（只读）。

### C-6 健康检查
**需求**：提供存活探针，供部署探活。**注：连接器已不依赖本端点（连接失败靠重试兜底），保留给运维探活用。**
**能力**：`GET /health` → `{"status":"ok"}`。
**数据**：无。

### C-7 参数校验与错误码
**需求**：非法输入系统级拦截，规则即边界。
| 场景 | 响应 |
|---|---|
| single 目标未注册 | 400 且不保存 |
| 未注册 Agent 调 reply | 400 |
| 空名 / 空内容 | 422（Pydantic） |
| 同名重注册 | 200 + already_exists |
| 重复 client_msg_id | 200 + duplicate:true |

---

## 五、D 类 · 数据与可靠性

### D-1 持久化三件套
**需求**：全部状态落盘三份 JSON，重启服务/机器不丢聊天记录。
**数据**：`data/agents.json`（注册与会话态）、`data/messages.json`（全部消息）、`data/reads.json`（per-agent 已读集合）。

### D-2 原子写与并发安全
**需求**：多 Agent 并发读写不撕裂、不丢数据。
**能力**：临时文件 + 原子 replace + 全局锁；并发写串行化。

### D-3 损坏自愈
**需求**：文件损坏不导致服务崩溃。
**能力**：读取异常时自动备份为 .corrupt 并自愈重建。

### D-4 环境隔离
**需求**：技能本地缓存目录可配置（`AGENT_HUB_DATA_DIR`），支持接入其它实例 / 跨机 / 隔离测试；默认路径不存在时自动回退 `~/.agent_hub/data`。

---

## 六、E 类 · 运维与部署

### E-1 启动
**需求**：一条命令起服务，跨平台脚本。
**能力**：`uvicorn app.main:app --host 0.0.0.0 --port 8000`；run.sh（mac/linux）/ run.bat（Windows）；依赖 fastapi/uvicorn/pydantic。

### E-2 冒烟测试
**需求**：启动后一键验证核心链路（注册→发消息→拉取→回复→已读）。
**能力**：`server/test_smoke.py`。

### E-3 净重启
**需求**：uvicorn 无热重载，改 storage/message_store 后必须净重启（杀残留 PID 再起唯一进程，防端口竞争 10048）。

---

## 七、F 类 · 非功能与边界

### F-1 网络边界
**需求**：仅本机 / 内网部署，绝不上公网；信任边界 = 内网。
**边界**：若未来上公网必须补 token（P0 风险，登记在案）。

### F-2 无鉴权
**需求**：内网信任边界内不加鉴权，接入零门槛。

### F-3 数据合规
**需求**：运行时数据（data/、*.log、__pycache__）一律 .gitignore 排除，聊天数据不进版本库。

---

## 八、验收清单（MVP 主链路）

1. 打开网页 → 看到「小伙伴会议系统」、阿编状态点、消息区、输入区。
2. 下拉能选到已注册 Agent（含「@所有人」）。
3. 发消息给指定 Agent → 消息显示「○ 未读」→ Agent pull 后变「✓ 已读」。
4. @所有人 消息 → 每个 Agent 各自已读，显示 N/N → 全部读后「✓✓ 全部已读」。
5. Agent 回复 → 网页左侧气泡（Markdown 渲染）即时可见。
6. 阿编状态点随 init/pull/end 正确切换（处理中→待命→已收工），会话中 last_seen 超窗显示「已掉线·需重唤」。
7. 重启服务后历史消息仍在。
8. 未注册 Agent 发消息被 400 拦截；空内容被 422 拦截；重复 client_msg_id 不落重复。
9. 「开会 → 拉消息 → 回复 → 收工前再拉 → 结束会议」全链路走通。

---

## 九、路线图（待办 / 待决策）

- [ ] **多 Agent 评审工作流是否并入**（设计 v1.2 的 提问→方案→互评→合并 流程，待老板拍板）
- [ ] 断线自动重连（当前仅 pull 容错）
- [ ] `loop.py` 更丰富的 `--help`
- [ ] 多工具适配薄壳（OpenClaw / Trae）
- [ ] 聊天记录导出 / 会议纪要生成

---

*本文档由阿编（分析师）以用户故事方式整理，2026-08-17。产品经理可据此直接写设计文档。*
